{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make", // Command to run your build process (e.g., make)
            "group": {
                "kind": "build",
                "isDefault": true // Set this to true to make it the default build task
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}
